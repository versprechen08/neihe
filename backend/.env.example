# Database
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=neihe
DB_PASSWORD=neihe
DB_DATABASE=neihe

# Auth
JWT_SECRET=change-me-to-a-real-secret-in-production

# Email (Resend) — get a key at https://resend.com, free tier is plenty for dev.
# Leave unset to skip real sending; forgot-password will log the reset link instead.
RESEND_API_KEY=
EMAIL_FROM=内核 NèiHé <onboarding@resend.dev>

# App
NODE_ENV=development
PORT=3000

# Frontend
FRONTEND_URL=http://localhost:5173
